#ifndef stdc
#define stdc

#include <stdio.h>
#include <assert.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <math.h>
#include "graph.h"
#include "queue.h"
#include "methods.h"
#include "search.h"

#define max_edge 100
#define max_vertex 100

//void stats(char name[], char func[]);
//int* shortestPath(int u, int v, char algorithm[], char name[]);
//void read(graph *g, char name[]);

const static int dx[] = {0, 0, -1, 1};
const static int dy[] = {-1, 1, 0, 0};
const static char dr[] = {'W', 'E', 'N', 'S'};
//extern char dr[] = {'E', 'W', 'N', 'S'};


#endif
